module.exports = {
    declareVariable: function(input){
        // On the next line, declare a new variable called "createdVariable" and assign it the value of the input
        var createdVariable = input;
        return createdVariable;
    }
}